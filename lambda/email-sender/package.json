{
  "name": "sbg-email-sender",
  "version": "1.0.0",
  "description": "Email sender Lambda function for SBG portal using Gmail",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && zip -r function.zip dist node_modules"
  },
  "dependencies": {
    "nodemailer": "^6.9.7"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/nodemailer": "^6.4.14",
    "typescript": "^5.0.0"
  }
}
